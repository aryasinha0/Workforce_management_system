import 'package:flutter/material.dart';

class FieldConditionScreen extends StatelessWidget {
  const FieldConditionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Field Condition")),
      body: const Center(child: Text("Field Condition Screen")),
    );
  }
}
